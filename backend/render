services:
- type: web
  name: customer-relationship-dashboard
  runtime: python

  buildCommand: pip install -r requirements.txt

  startCommand: gunicorn run:app

  envVars:
    - key: MODE
      value: production

    - key: PYTHON_VERSION
      value: 3.12
